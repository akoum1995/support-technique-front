import { PersonnelPipePipe } from './personnel-pipe.pipe';

describe('PersonnelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonnelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
