import { ReclamationPipePipe } from './reclamation-pipe.pipe';

describe('ReclamationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReclamationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
