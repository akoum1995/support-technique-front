import { FilterReclamationsPipe } from './filter-reclamations.pipe';

describe('FilterReclamationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReclamationsPipe();
    expect(pipe).toBeTruthy();
  });
});
