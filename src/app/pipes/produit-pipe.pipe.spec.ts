import { ProduitPipePipe } from './produit-pipe.pipe';

describe('ProduitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProduitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
