import { FilterClientsPipe } from './filter-clients.pipe';

describe('FilterClientsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientsPipe();
    expect(pipe).toBeTruthy();
  });
});
