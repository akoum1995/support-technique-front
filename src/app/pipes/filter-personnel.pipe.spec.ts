import { FilterPersonnelPipe } from './filter-personnel.pipe';

describe('FilterPersonnelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPersonnelPipe();
    expect(pipe).toBeTruthy();
  });
});
